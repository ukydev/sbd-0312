version: 0.2

phases:
  install:
    commands:
      - echo Installing dependencies...
      - npm install

  build:
    commands:
      - echo "Running build"
      - echo "Hello World"  # Corrected command
      - npm run build

  post_build:
    commands:
      - echo "Build completed successfully"
